#include <stdio.h>
#include <string.h>

int main() {
    char str1[10], str2[20];
    scanf("%s %s",&str1,&str2);
    printf("Enter first string:%s\n ",str1);
    printf("Enter second string:%s\n ",str2);
    int result=strcmp(str1,str2);
    
    if (result > 0) {
        printf("%s is greater than %s is %d",str1,str2,result);
    } else if (result < 0) {
        printf("%s is smaller than %s is %d",str1,str2,result);
    } else {
        printf("%s and %s both are equal is %d",str1,str2,result);
    }

}
