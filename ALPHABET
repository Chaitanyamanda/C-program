#include <stdio.h>
void main()
{
  char ch;
  scanf("%c",&ch);
  if((ch>='A' && ch<='Z')||(ch>='a' && ch<='z'))
  {
    printf("character is alphabet");
    
  }
  else{
    printf("character is not alphabet");
  }
}
