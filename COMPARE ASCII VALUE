#include <stdio.h>
int main()
{
    char ch1,ch2;
    scanf("%c %c",&ch1,&ch2);
    printf("ASCII value of %c is %d\n",ch1,ch1);
    printf("ASCII value of %c is %d\n",ch2,ch2);
    if(ch1>=ch2){
      printf("login is successfull!!");
    }
}
