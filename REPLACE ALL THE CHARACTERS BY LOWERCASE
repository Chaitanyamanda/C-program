#include <stdio.h>
int main() {
    char str[10]; 
    scanf("%s",&str);
    printf("string:%s\n ",str);
    for (int i = 0; str[i] != '\0'; i++) {
        str[i] = tolower(str[i]);
    }

    printf("Lowercase string: %s", str);
    return 0;
}
